# 07 — Marquee text line (ticker)

## Reference used
Block 6 "GSAP horizontal ticker" in `reference_code.md` + `design_preview/06_ticker.html`.

## Adaptation notes
- Replicated the GSAP ScrollTrigger pinned horizontal text-flow: single flex `track`, `x: -scrollWidth` scrubbed, `pin:true`.
- Inline SVG curve/star/clock icons as punctuation between words (like source).
- Rewrote the sentence to Arranto copy: "We design the undeniable motion that makes premium websites feel alive."
- Accent (lime) on key words; removed the source's purple `#7c3aed` / pink `#db2777`.

## Open questions
- None. Sentence can be swapped for a client testimonial-style line if preferred.
