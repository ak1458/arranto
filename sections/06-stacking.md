# 06 — Portfolio / services cards (stack & slide)

## Reference used
Block 5 "Stacking Cards" in `reference_code.md` + `design_preview/05_stacking.html`.

## Adaptation notes
- CSS `position:sticky; top:16vh` per card; each subsequent card slides over the previous as you scroll — exactly the deck-stack behavior.
- 4 cards populated with the user's real products (FatooraLite, Tuition Mandi, Veloria Vault, Attest) from memory + the stacking demo copy.
- Recolored from the source's per-card purple/pink/cyan gradients to the unified dark-studio surface with a single lime left-edge accent (one accent, used once per card — restraint).
- Each card carries `.card-hover` (Section 2 jiggle). Tech chips added for specificity.

## Open questions
- Confirm the 4 listed products are the right "selected work" set, or swap in others.
- Real card imagery (currently solid surface + chips).
