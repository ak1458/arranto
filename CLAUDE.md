@AGENTS.md

Read `docs/MASTER-CONTEXT.md` FIRST — it is the single source of truth for build state, decisions, and remaining tasks. Update its checkboxes as you complete work.

Key rules (full set in MASTER-CONTEXT §9): server components default; CSS logical properties only; YAGNI/shortest-correct-solution; no fabricated metrics — status claims read from `src/content/work.ts` `status` field; chatbot never calls in-pilot products live; no CAPTCHA; no ad pixels.

Setup/external services: `docs/SETUP.md`. Self-improvement agent: `docs/AGENT.md`.
